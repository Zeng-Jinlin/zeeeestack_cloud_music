import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.zeeeestack.cloudmusic',
  appName: '个人音乐平台',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
